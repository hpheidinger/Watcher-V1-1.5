# - geo.conf -
# Configuration file for dynloader 'geo'
ZONE_DIR=zones
ZONES='cn ru kp by er br in za ir eg et'
# Extended BRICS-friendly countries ...
ZONES='cn ru kp by er br in za ir eg et ae sa ar sy cu ve vn id pk ml sd zw cd'


WGET='wget -q --no-check-certificate'
UPD_URL='https://ipdeny.com/ipblocks'
DAT_URL='https://ipdeny.com/ipblocks/data/aggregated'
AGG_SUF='-aggregated.zone'

#
## Do not change below
#
# IPSET basics ...
SETTYPE='hash:net'
SETOPTS='comment counters'

### Database
DB=$ME.db
SQL="sqlite3 $DB"
# DB_PATH="$POOL/geo.sqlite"	# SQLite DB location (RAMdisk)
SCHEMA_FILE="Schema.sql"	# Relative or absolute path
USE_SQLITE=1			# Flag to activate DB logic

COUNTER_TRACKING=1		# Enable counter persistence
DELTA_LOGGING=1			# Enable ipset_history

